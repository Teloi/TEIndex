export const environment = {
  production: true
};

export class Environment {
  // static readonly serverUrl = 'http://tjserver.esoosoft.com/';
  // static readonly serverApiUrl = Environment.serverUrl + 'api/';
  // static readonly clientUrl = 'http://tongji.esoosoft.com/';
  // static readonly clientSrcUrl = Environment.clientUrl;
}
